<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>User Profile Card</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css"
    />
    <link
      rel="stylesheet"
      href="https://pro.fontawesome.com/releases/v5.10.0/css/all.css"
    />
    <style>
     
      .user-image {
        width: 50px;
        height: 50px;
        border-radius: 50%;
        object-fit: cover;
        margin-top: 2px;
      }
      .user-name {
        font-size: 24px;
        font-weight: bold;
        /* margin-bottom: 10px; */
      }
      .user-email {
        font-size: 18px;
        margin-bottom: 10px;
        color: #999;
      }
      .logout-button {
        /* width: 100%; */
        background-color: lightgreen;
        color: #fff;
        text-align: center;
        margin: 10px 20px;
        padding: 10px 10px;
        border-radius: 5px;
        text-decoration: none;
      }
      nav{
        background-color: rgba(145, 139, 139, 0.25);
        overflow: hidden;
      }
      nav ul{
        list-style: none;
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: end;
      }
      nav li{
        /* float: right; */
      }
      nav a{
        display: block;
        color: rgb(22, 21, 21);
        text-align: center;
        padding: 14px 16px;
        text-decoration: none;
      }
      nav :nth-child(3) a:hover{
        background-color: #1d9b38;
        color: black;
      }
    </style>
  </head>
  <body>
    <nav>
      <ul>
        <li><a href="#"><img
          class="user-image"
          src="<%= user.photos[0].value %>"
          alt="User Image"
        /></a></li>
        <li> <a  class="user-name"><%= user.displayName %></a></li>
        <li> <a href="/logout" class="logout-button">Logout</a></li>
      </ul>
    </nav>
   
  </body>
</html>
